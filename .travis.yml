language: php
php:
    - "5.4"
    - "5.5"
    - "5.6"

env:
    global:
    matrix:
        - CONTAO_VERSION=~3.2
        - CONTAO_VERSION=~3.3

install:
    - composer require contao/core $CONTAO_VERSION
    - composer install --prefer-dist --dev

script: ant -keep-going
